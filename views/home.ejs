<%- include('partials/header') -%>



    <h1>Home</h1>

    <% post.forEach((post) => { %>
        <h3>
            <%= post.postTitle %>
        </h3>
        <p>
            <%= post.postContent.substring(0, 100) + " ..."%>
            <a href="/posts/ + <%= post.postTitle %>"><em>Read More</em></a>
        </p>
        
        <% }) %>

<%- include('partials/footer') -%>
